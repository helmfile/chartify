apiVersion: v2
# The important point is that this `chartName` does't match with
# the directory name `localchart`.
# This resulted in https://github.com/variantdev/chartify/pull/13#issuecomment-1077431214 before.
name: chartname
description: A Helm chart for Kubernetes
type: application
version: 0.1.0
appVersion: "1.16.0"
