apiVersion: core/v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-vers
spec:
  kubeVersion: {{ .Capabilities.KubeVersion }}
  {{ range $i, $v := .Capabilities.APIVersions -}}
  apiVersion{{ $i }}: {{ $v }}
  {{ end -}}
